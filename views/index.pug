extends templates/main
block Content
	.card
		img.card__image(data-src="/images/logo.png", title=APP_NAME, alt=`${APP_NAME} logo`)
		h1.card__content Hi, this is a boilerplate made with NodeJS, Express, Sass, Pug and other stuff.

block Scripts
	script.
		window.addEventListener('DOMContentLoaded', () => {
			LazyLoad.observe('img');
		})